import test from 'node:test';import assert from 'node:assert/strict';import {createPlatform} from '../server/index.js';
test('production HTTP serves health, bilingual catalog and SPA routes',async()=>{const p=createPlatform();await new Promise(r=>p.http.listen(0,'127.0.0.1',r));const base=`http://127.0.0.1:${p.http.address().port}`;try{assert.deepEqual(await(await fetch(base+'/api/health')).json(),{ok:true});const c=await(await fetch(base+'/api/catalog')).json();assert.equal(c.locations.length,24);assert.ok(c.locations.every(l=>l.th&&l.en&&!l.roles));const html=await(await fetch(base+'/room/123456')).text();assert.match(html,/<title>Tiny Together<\/title>/);const asset=html.match(/src="([^\"]+\.js)"/)[1];assert.equal((await fetch(base+asset)).status,200);}finally{await p.close();}});
